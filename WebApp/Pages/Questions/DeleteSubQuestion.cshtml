@page
@model WebApp.Pages.Questions.DeleteSubQuestion

@{
    ViewData["Title"] = "Delete  Sub Question";
    Layout = "~/Pages/_Layout.cshtml";
}

<div>
    <h4>Are you sure you want to delete this Sub Question?</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Question.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Question.Title)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Question.QuestionId" />
        <input type="hidden" asp-for="Question.ParentId" />
        <input class="btn btn-success" type="submit" value="Delete" />
        <a asp-page="./SubQuestions" asp-route-id="@Model.Question.ParentId">Back to List</a>
    </form>
</div>
